// CurvePoint
export class CurvePoint {
    // coord
    public x: number = 0.0;
    public y: number = 0.0;
    // constructor
    constructor(x: number, y: number) {
        this.x = x;
        this.y = y;
    }
}